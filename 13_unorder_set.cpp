#include <bits/stdc++.h>
using namespace std;
 
// just like set we've multiset which allows duplicates & is sorted

// unordered_set doesnt allow duplicates & all eles are not sorted
// and here we've unordered_set : Time Complexity: O(1)

// all methods : emplace() push() erase() empty() size() find()
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    unordered_set<int> s;
    return 0;
}